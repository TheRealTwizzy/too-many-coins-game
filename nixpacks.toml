[phases.setup]
nixPkgs = ["php", "phpExtensions.pdo", "phpExtensions.pdo_mysql", "phpExtensions.mysqli"]

[phases.build]
cmds = [
 "mkdir -p public",
 "mkdir -p public/api"
]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t public public/index.php"
